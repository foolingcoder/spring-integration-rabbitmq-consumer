server:
  port: 8082

spring:
  cloud:
    stream:
      bindings:
        output:
          destination: lines-in
        input:
          destination: lines-out
      rabbit:
        bindings:
          output:
            producer:
              exchangeType: direct
              routingKeyExpression: '"#"'